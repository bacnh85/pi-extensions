export const PLAN_MODE_SERENA_GUIDANCE = "Tool selection for code research: for source files, symbols, functions, classes, declarations, references, implementations, and refactors, use Serena before raw reads/searches. Use serena_get_symbols_overview for source-file outlines; serena_find_symbol for named functions/classes/methods/variables; serena_find_referencing_symbols before behavior changes or renames; and serena_find_declaration / serena_find_implementations for definitions, interfaces, and implementations. Use read for docs/config/non-code files, exact line ranges, or after Serena identifies the relevant code region. Prefer the dedicated ls/ffgrep/fffind tools (FFF: frecency-ranked, paginated) over shell commands; fall back to grep/find when the fff tools are unavailable. Strict single read-only shell commands run automatically; test, build, and package scripts require confirmation because they may modify files. Use grep/find for exact text or file searches when semantic lookup is not the right fit. In plan mode, pipelines and chains of read-only commands (grep, rg, find, fd, ls, cat, head, tail, wc, sort, uniq, cut, echo, printf, jq, strings, stat, file, print-only sed, xargs over read tools, tar -t/-xO, read-only git subcommands) plus cd && chains and VAR=value prefixes run without prompting; $(), backticks, file redirects, heredocs, interpreters like python3 -c / node -e, and test/build/package scripts prompt or hard-block — keep research to the auto-run forms.";
