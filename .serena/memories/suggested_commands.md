Useful commands on Darwin: `rg --files`, `rg <pattern>`, `find . -maxdepth 2 -type d`, `git status --short`, `git diff`, `npm test --prefix pi-web` (replace package), `npm pack --dry-run ./pi-rtk`, `npm pack --dry-run ./pi-sub`, `pi -e ./pi-web` for local entrypoint testing. Most packages run tests with `cd extensions && npx mocha`; pi-ponytail runs `node --test extensions/test/*.test.js`. Some packages expose `typecheck: tsc --noEmit`. Evidence: root README.md and package.json files.