Extensions default-export a function accepting `(pi: ExtensionAPI)`, register tools with TypeBox schemas, and register commands/hooks as needed. Root AGENTS.md requires minimal focused diffs, preserve APIs, use existing helpers, avoid new dependencies, use structured parsers, add a runnable regression check for non-trivial logic, and follow the ponytail YAGNI/stdlib-first ladder. Skills live under skills/<name>/SKILL.md with YAML frontmatter. Evidence: root AGENTS.md and package layouts.