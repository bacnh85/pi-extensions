When completing work: inspect current git status and diff, run the narrowest package test, then broader typecheck/build checks when contracts are affected. Do not claim checks passed unless executed; report blockers exactly. Releases bump a package version, then merge to main for GitHub Actions matrix testing and publishing. Evidence: root AGENTS.md, README.md, and .github/workflows/.